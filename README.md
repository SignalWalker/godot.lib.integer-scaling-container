# integer-scaling-containers

A tiny Godot extension that adds a control container that ensures that its child nodes are always scaled by an integer, so that, for example, pixel art doesn't get scaled badly.
